async function loadAdmin(){try{const r=await fetch(API_BASE+'/parking-slots');const slots=await r.json();const av=slots.filter(s=>String(s.status).toUpperCase()==='AVAILABLE').length;adminStats.innerHTML=`<div><b>${slots.length}</b><span>Total Slots</span></div><div><b>${av}</b><span>Available</span></div><div><b>${slots.length-av}</b><span>Occupied</span></div><div><b>₹ —</b><span>Today's Revenue</span></div>`;adminSlots.innerHTML=slots.map(s=>`<div class="slot ${String(s.status).toLowerCase()}"><strong>${s.slotNumber}</strong><small>${s.vehicleType}</small><small>${s.status}</small></div>`).join('')}catch(e){adminSlots.innerHTML='<div class="empty">API unavailable</div>'}}
loadAdmin();
